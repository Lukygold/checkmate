<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="reservationMapper">

	<resultMap type="room" id="roomResultSet">
		<result column="ROOM_NO" property="roomNo"/>
		<result column="ROOM_CENTER_NAME" property="roomCenterName"/>
		<result column="ROOM_NAME" property="roomName"/>
		<result column="ROOM_SIZE" property="roomSize"/>
	</resultMap>
	
	<select id="home" resultMap="roomResultSet">
		SELECT ROOM_CENTER_NAME
		FROM ROOM
		GROUP BY ROOM_CENTER_NAME
		ORDER BY ROOM_CENTER_NAME
	</select>
	<select id="selectRoomSize" resultMap="roomResultSet">
		SELECT ROOM_SIZE
		FROM ROOM
		WHERE ROOM_CENTER_NAME = #{roomCenterName}
		GROUP BY ROOM_SIZE
		ORDER BY ROOM_SIZE
	</select>  	
	<select id="selectRoomName" resultMap="roomResultSet">
		SELECT ROOM_NAME
		FROM ROOM
		WHERE ROOM_CENTER_NAME = #{roomCenterName}
		AND ROOM_SIZE = #{roomSize}
		ORDER BY ROOM_NAME
	</select>
</mapper>
